# Linux/Python 3.12 host installation for the dense two-H100 run.
# Install CUDA PyTorch first, then this file (see README).
# Halo and gram-newton-schulz are installed separately with --no-deps:
# Halo's Muon dependency pins CUDA 12.9 bindings, incompatible with torch cu130.
# Qwen3.5 uses the PyTorch conv1d fallback when nvcc/causal-conv1d is absent.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.11.0+cu130
torchvision==0.26.0+cu130
transformers==5.16.1
datasets>=4.8.5,<5.0.0
accelerate>=1.11.0,<1.12.0
trl>=1.6.0,<1.7.0
peft>=0.18.1,<0.19.0
liger-kernel>=0.8.0,<0.9.0
scikit-learn>=1.6.0,<2.0.0
flash-linear-attention==0.4.2
clearml>=2.0.2,<3.0.0
editables>=0.5,<1.0
